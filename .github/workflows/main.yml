name: Build & Pack MCP Server Tool

on:
  workflow_dispatch:
    inputs:
      projectName:
        description: "Project folder & csproj name"
        required: true
        default: "EtServerChan.McpServer"
      packageVersion:
        description: "NuGet Package Version"
        required: true
        default: "0.1.0-beta"

env:
  DOTNET_VERSION: 10.0.x
  CONFIGURATION: Release
  PROJECT: src/${{ inputs.projectName }}/${{ inputs.projectName }}.csproj
  PACKAGE_VERSION: ${{ inputs.packageVersion }}

jobs:
# ------------------------------------------------------------
# Pack & Validate
# ------------------------------------------------------------
  pack:
    name: Pack & Create Release & upload NuGet
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          include-prerelease: true

      # ---------- pack ----------
      - name: Pack
        run: |
          dotnet pack $PROJECT \
            -c $CONFIGURATION \
            -p:PackageVersion=$PACKAGE_VERSION \
            -o artifacts

      # ---------- GitHub Release ----------
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.PACKAGE_VERSION }}
          name: v${{ env.PACKAGE_VERSION }}
          files: artifacts/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ---------- optional NuGet push ----------
      # - name: Push to NuGet.org
      #   run: |
      #     dotnet nuget push artifacts/*.nupkg \
      #       --api-key ${{ secrets.NUGET_API_KEY }} \
      #       --source https://api.nuget.org/v3/index.json \
      #       --skip-duplicate
